Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/rna_picard.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n rna_picard
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/rna_picard/bin:$PATH"

%runscript
    exec "$@"