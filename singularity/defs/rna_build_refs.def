Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/rna_build_refs.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n rna_build_refs
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/rna_build_refs/bin:$PATH"

%runscript
    exec "$@"