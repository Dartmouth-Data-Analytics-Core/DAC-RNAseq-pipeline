Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/rna_pcaplot.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n rna_pcaplot
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/rna_pcaplot/bin:$PATH"

%runscript
    exec "$@"