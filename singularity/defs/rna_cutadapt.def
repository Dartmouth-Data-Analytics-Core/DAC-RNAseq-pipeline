Bootstrap: docker
From: condaforge/mambaforge:latest

%files
    ../../env_config/rna_cutadapt.yaml /env.yaml

%post
    mamba env create -f /env.yaml -n rna_cutadapt
    mamba clean --all -y

%environment
    export PATH="/opt/conda/envs/rna_cutadapt/bin:$PATH"

%runscript
    exec "$@"